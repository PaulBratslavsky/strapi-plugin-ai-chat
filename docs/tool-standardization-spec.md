# Tool Standardization Spec — moved

This was the pre-implementation spec for the hub-and-extensions model. It
shipped, and its contents have been folded into the current reference.

**Current reference:** [plugin-contract.md](./plugin-contract.md) — the
`ai-tools` service, `ToolDefinition`, Zod rules, namespacing, naming
conventions, and the extension-plugin checklist.

The original document is preserved unchanged at
[`old/tool-standardization-spec.md`](./old/tool-standardization-spec.md).
